{
  "pluginAlias": "SonoffBasicR3",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Sonoff BasicR3 Platform"
      },
      "refreshRate": {
        "title": "Refresh Rate",
        "description": "The interval in seconds between accessory state polls",
        "type": "integer",
        "default": 15
      }
    }
  }
}